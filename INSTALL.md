# INSTALL.md — 設置と初期設定

AIエージェントがこの手順を上から実行します。**各ステップの「確認」が通ってから次へ進んでください。**
通らなかったら、その場で止めて人に報告します。飛ばさないでください。

前提: PHP 7.4 以上が動くサーバー。データベースは要りません。

---

## 0. 環境を確かめる

推測せず、実際に叩いて確かめます。

```bash
php -v                                    # 7.4 以上か
php -m | grep -iE "mbstring|json|session" # 3つとも出るか
```

**確認**: PHPのバージョンが 7.4 以上で、`mbstring` / `json` / `session` が有効。

出ない場合は、その時点で人に報告してください。`mbstring` が無いと日本語が壊れます。

---

## 1. ファイルを置く

`public/` の中身を、Webで見える場所へ置きます（FTP・SFTP・`cp` いずれでも）。

```
置き先/
├─ kinvoice.php
├─ kinvoice_dl.php
├─ kinvoice_auth.php
├─ kinvoice_lib.php
├─ kinvoice_pdf.php
├─ kinvoice_config.php.example
└─ kinvoice_data/
   └─ .htaccess          ← 消さないこと
```

**確認**: ブラウザまたは `curl` で `kinvoice.php` を開き、
「**初期設定が終わっていません**」と、不足している設定名が表示される。

```bash
curl -s "https://あなたのドメイン/kinvoice.php" | grep -o "初期設定が終わっていません"
```

ここで PHP のエラーが出る場合は、PHPのバージョン指定を疑ってください。
共有サーバーでは `.htaccess` でバージョンを選ぶことがあります（例: `AddHandler php8.2-script .php`）。

---

## 2. 台帳が書けるか確かめる

`kinvoice_data/` は自動で作られますが、書き込み権限が必要です。

**確認**: 台帳ディレクトリがWebから読めないこと。**403 が正解です。**

```bash
curl -s -o /dev/null -w "%{http_code}\n" "https://あなたのドメイン/kinvoice_data/"
```

**200 が返ったら止めてください。** 顧客名・メールアドレス・金額が外から読める状態です。
`.htaccess` が効いていないので、サーバー側の設定（`AllowOverride`）を人に確認します。

---

## 3. 管理パスワードを作る

```bash
php scripts/make_password_hash.php 'ここに決めたパスワード'
```

`define('KINV_ADMIN_PASSWORD_HASH', '$2y$10$...');` という1行が出ます。

**平文のパスワードはどこにも保存しないでください。** 人に口頭かパスワードマネージャで渡します。

---

## 4. 設定ファイルを作る

`kinvoice_config.php.example` をコピーして `kinvoice_config.php` を作り、値を入れます。

```php
<?php
define('KINV_ADMIN_PASSWORD_HASH', '手順3で出た文字列');
define('KINV_NAME', '発行元の会社名');
define('KINV_MAIL_FROM', 'メールの送信元アドレス');

// 適格請求書発行事業者の登録番号。取得していなければ、この行を書かない。
// define('KINV_INVOICE_REG_NO', 'T1234567890123');
```

**架空の登録番号を入れないでください。** 未設定なら「記載しておりません」と印字されます。

**確認**: `kinvoice.php` を開くと、初期設定の案内が消えて**パスワード入力欄**が出る。
ヘッダーに設定した会社名が表示される。

---

## 5. メールが本当に届くか確かめる（ここが一番よく失敗します）

`mail()` は**送れなくても `true` を返します。**「送信済」の表示は、届いた証明になりません。

まず送信ドメインのSPFを見ます。

```bash
dig +short TXT あなたのメールのドメイン | grep -i spf
```

そのサーバーがSPFに含まれていないと、受信側（Gmail等）に捨てられます。
共有サーバーなら、**そのサーバーの会社が案内しているドメインのアドレス**を
`KINV_MAIL_FROM` に使ってください。

**確認**: 管理画面から、**自分のアドレス宛に**テストの領収書を1通発行する。

- [ ] メールが届いた（迷惑メールフォルダも見る）
- [ ] メール内のURLを開くとダウンロードページが出る
- [ ] **正しいメールアドレス**を入れるとPDFが落ちる
- [ ] **違うメールアドレス**では落ちない

4つ全部が通って初めて完了です。届かなければ `KINV_MAIL_FROM` を変えて再送します。

---

## 6. 人に伝える

- 管理画面のURL
- 管理パスワード（安全な手段で）
- 「領収書PDFはメールに添付されない。お客様はURLとメールアドレスで受け取る」という仕組みの説明

---

## うまくいかないときに見るところ

| 症状 | 見るところ |
|---|---|
| 画面が真っ白／文字化け | PHPのバージョン指定、`mbstring` |
| 「初期設定が終わっていません」が消えない | `kinvoice_config.php` の置き場所とファイル名 |
| 設定を書いたのに反映されない | `kinvoice_lib.php` より先に読まれているか（`define` は先勝ち） |
| メールが届かない | **SPF。** `KINV_MAIL_FROM` のドメイン |
| 台帳が 200 で見える | `.htaccess` が効いていない。サーバー設定 |
| ダウンロードURLのドメインが違う | `KINV_BASE_URL` を設定する |

より詳しい失敗の記録は `docs/02-failures.md` にあります。
