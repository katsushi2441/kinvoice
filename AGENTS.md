# AGENTS.md — AIエージェントへの指示

**このファイルを最初に読んでください。** これは「領収書をPDFで発行し、ダウンロードURLをメールで送る」
システムの一式です。あなた（AIエージェント）が設置と初期設定を行い、人が使える状態にします。

## あなたがやること

1. この文書を最後まで読む
2. `INSTALL.md` の手順を上から実行する
3. 各手順の「確認」を必ず実行し、通ってから次へ進む
4. 全部通ったら、管理画面のURLと管理パスワードを人に伝える

**確認していないものを「できました」と言わないでください。** この一式は
`vwork/VERIFICATION.md` の作法に従って作られています。

## このシステムの中身

| ファイル | 役割 |
|---|---|
| `public/kinvoice.php` | 管理画面。領収書を発行してメールを送る。送信履歴もここ |
| `public/kinvoice_dl.php` | お客様がPDFを受け取るページ |
| `public/kinvoice_auth.php` | 管理者ログイン（既定はパスワード） |
| `public/kinvoice_lib.php` | 台帳・税計算・メール送信 |
| `public/kinvoice_pdf.php` | 領収書PDFの生成（外部ライブラリを使わない） |
| `public/kinvoice_config.php` | **設置する人が作る設定ファイル。**リポジトリには入っていない |

## 動作要件

- **PHP 7.4 以上**（8.x で動作確認済み）
- `mail()` が使えること（送信できないサーバーもあるので `INSTALL.md` で確認します）
- **データベースは不要**。JSONファイルに `flock` で書きます
- 常駐プロセス・cron・Composer・npm はすべて不要

## 設計上、勝手に変えてはいけないこと

以下は理由があってこうなっています。**変更する前に、人に確認してください。**

- **PDFをメールに添付しない。** 宛先を間違えた瞬間に領収書が他人の手元に残るためです。
  推測困難なURL（32桁）＋宛先メールアドレスの確認、の2つが揃って初めて開きます
- **ダウンロードページは認証前に何も出さない。** 顧客名・金額・領収書番号を出すと、
  URLだけが漏れたときに「誰宛のいくらか」が分かってしまいます
- **税額の内訳は、合計が必ず税込総額に一致する。** 税抜を切り捨て、差額を消費税に寄せています。
  1円ズレると領収書として使えません
- **`kinvoice_data/` の `.htaccess` を消さない。** 台帳には顧客名・メール・金額が入ります
- **登録番号が未設定なら印字しない。** 架空の番号を書かないためです

## 学ぶための資料

このパッケージは、動くシステムであると同時に**バイブコーディングの教材**です。

- `docs/01-overview.md` — 何を、どういう判断で作ったか
- `docs/02-failures.md` — **実際に踏んだ6つの罠と、気づき方**（ここが本体です）
- `docs/03-exercises.md` — 請求書・見積書へ改造する練習

## VWork フレームワーク

`vwork/` に、AIと仕事を進めるための作法が入っています。この一式もこの作法で作られました。

1. `vwork/WORK_PROTOCOL.md` — 目的・成果物・確認・記録の流れ
2. `vwork/RULES.md` — 禁止事項、確認が必要な操作、秘密情報の扱い
3. `vwork/VERIFICATION.md` — 「できました」と言う前の確認手順
4. `vwork/SERVERS.md.sample` — 環境・データ・公開先を書き留める雛形

**改造を頼まれたときは、まず `vwork/RULES.md` と `vwork/VERIFICATION.md` を読んでください。**
