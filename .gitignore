public/kinvoice_config.php
# 領収書の台帳には顧客名・メール・金額が入る。絶対にコミットしない。
public/kinvoice_data/*
!public/kinvoice_data/.htaccess
outputs/
*.pyc
__pycache__/

# ── ここから下は「有償パッケージにだけ同梱する」もの ──
# GitHubからOSSとして取得する人にはコードだけを渡す。
# 教材とVWorkは、ダウンロード販売の価格の根拠なので公開しない。
# 削除はしない。ローカルには置いたまま、scripts/build_package.sh で束ねる。
vwork/
docs/
AGENTS.md
CLAUDE.md
INSTALL.md
SUPPORT.md
